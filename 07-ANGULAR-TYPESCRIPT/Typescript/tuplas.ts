// tupla es un array de longitud fija y no tengo obligación de uq esean del mismo tipo.

const array: string[] = ['1', '0','2'];

let tupla: [string, string | number, boolean] = ['Juan', 40, true];
console.log(tupla);

let nombre: string | number = 'Juan';